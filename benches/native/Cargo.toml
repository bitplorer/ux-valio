[package]
name = "ux-valio-peer-bench"
version = "0.0.0"
edition = "2021"
license = "MIT"
publish = false
description = "Measure-only PyO3 stub: compile once + apply(IntDoor, Ge(0)). Not ux-valio[native]."
rust-version = "1.83"

[lib]
name = "ux_valio_peer_bench"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29.2", features = ["extension-module"] }
